{
  "name": "notes-app-client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "aws-amplify": "^0.3.0",
    "react": "^16.3.1",
    "react-bootstrap": "^0.32.1",
    "react-dom": "^16.3.1",
    "react-router-bootstrap": "^0.24.4",
    "react-router-dom": "^4.2.2",
    "react-scripts": "^1.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "npm run build",
    "deploy": "aws s3 sync build/ s3://arm-notes-app-client --delete",
    "postdeploy": "aws cloudfront create-invalidation --distribution-id EMBFKU4FNFLYM --paths '/*' && aws cloudfront create-invalidation --distribution-id E2IUCYGWU7X0FP --paths '/*'",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "eslint": "^4.19.1"
  }
}
